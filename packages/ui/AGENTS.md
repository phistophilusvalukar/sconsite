# Shared UI rules

- Components must remain renderer-agnostic and keyboard/screen-reader accessible.
- Never infer or mutate canonical match state; render supplied views and emit callbacks.
- Essential game information must be available as text, not color or canvas alone.
